#include <stdio.h>

/**
 * main - prints the name of this programme
 *
 *@argc: argument count of the programe
 *@argv: argument vector of the progrmme
 *
 *Return: 0 for success
 */

int main(int __attribute((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
