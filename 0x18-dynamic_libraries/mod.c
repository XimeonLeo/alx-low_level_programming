#include "main2.h"

/**
 * mod - gets the module of a number
 *
 * @dgt1: the fitst number
 * @dgt2: the second number
 *
 * Return: the remainder when a number divides another
 */

int mod(int dgt1, int dgt2)
{
	return (dgt1 % dgt2);
}
